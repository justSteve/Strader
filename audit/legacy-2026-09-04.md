# Strader legacy-clutter audit — 2026-09-04 (st-2opj; Desk work order 20260904T150100; dispositions await COO)

**EXECUTED 2026-09-06** — see `docs/plans/2026-09-04-prune-tranche-1.md` for what moved, the five rows this audit had wrong, and the one step held back. The `pre-prune-2026-09-05` tag holds every removed file. [st-rfjg]

Report-only as WRITTEN on 2026-09-04: nothing in the repo was edited, moved, tagged or deleted then. Every row below was verified on
2026-09-04 with the commands named in the Method section; "live refs" means referencing tracked files
after excluding the file itself, `DaysActivity.md` and `archive/DaysActivity-*`, `docs/a2a/`,
`CurrentStatus.md`, `session-review-*`, and the 08-12 census JSON (which mentions everything).

**Context the reader needs first.** Strader already ran a code-estate audit on 2026-08-12 (st-nujt,
`docs/audits/2026-08-12-code-estate/dead-verdicts.json`: 22 Strader files confirmed-dead, 30 ambiguous).
Its execution bead **st-sl1f ("loud-stub corpus_poll.py, delete 22") is still OPEN, owner Steve, untouched
since 08-12**, and all 22 files are still in HEAD. This audit re-verifies that tranche (18 of 22 confirmed
again; 4 corrected to keep), rules on the 30 ambiguous, and adds what the first pass did not cover:
configs, experiment directories, docs, deprecated-name mentions, memory, and commit archaeology.

## Summary

| Category | Rows | keep | archive | delete | fix-in-place | unsure-flagged |
|---|---|---|---|---|---|---|
| 1. Dead scripts and modules | 52 | 17 | 14 | 21 | 0 | 13 |
| 2. Superseded configs | 12 | 4 | 0 | 7 | 1 | 4 |
| 3. Orphaned experiment dirs / Gas City remnants | 11 | 4 | 4 | 2 | 1 | 2 |
| 4. Stale documentation | 13 | 5 | 0 | 0 | 8 | 2 |
| 5. Deprecated names presented as current | 20 mentions (11 current / 9 history) | — | — | — | — | 0 |
| 6. Memory clutter | 22 | 11 | 0 | 9 | 2 | 7 |
| 7. Commit archaeology | 13 commits | informational | | | | |
| **Total dispositioned rows** | **110** | **41** | **18** | **39** | **12** | **28** |

Category 5 is a cross-reference table: every one of its 20 mentions is dispositioned by a row in
§1–§4 (row 74 carries the two Gas City remnants that had no other home), so it adds nothing to the
totals. "Unsure-flagged" rows keep their default disposition and are listed in the Unsure list below
(16 adjudication items covering the 28 flagged rows).

Scale of the prune if every delete/archive above is taken: ~143 tracked files (of 1,103). 77 of them
are six whole directories or packages — tui/ (16), the domain-factory skills (12), mancini/archive (35),
the dead mancini modules (6), daemon/ (4), the supervisor wrappers (4) — and the rest are single
scripts. Nothing scheduled (15 crontab lines, 9 enabled systemd units) is touched by any row.

### Unsure — items a human must adjudicate (one line each)

1. **The 2026-05-17 entity/indicator stratum** (rows 34–37: `market/indicators/*`, `market/entities/{gex_profile,position}.py`, `market/ingest/{__init__,mancini,schwab}.py`, `market/pricing/black_scholes.py` + 10 green tests) — only consumers are dead scripts; the 08-12 plan put this to Steve as "Decision 2" and it was never answered.
2. **Gate-hook test fixtures** (row 41): `scripts/gex_now.py`, `gex_series.py`, `hello_schwab.py` are dead but `tests/test_schwab_gate_hook.py` reads them as fixtures, and the hook skips a missing file (`[ -f "$PY_FILE" ] || continue`) — delete them and two "must BLOCK" cases silently flip to allow. Move the fixture first.
3. **Live-parity wrapper** (row 42): never scheduled (crontab confirmed 09-04); st-hrwe OPEN since 08-12 — install or retire, one word.
4. **Retired supervisor wrappers** (row 25): four `scripts/cron/*supervisor*.sh` retired 08-13 for systemd timers, still carrying two tests — archive, or keep as manual fallback?
5. **`strader/feeds/__init__.py`** (row 32): the documented "single seam over carried infra" that nothing calls — delete, or keep as intended architecture?
6. **`strader/evaluate/*`** (row 39) and **`market/orderflow/regime.py`** (row 40): test-only modules with design intent behind them (playbook evaluator; Steve's 08-05 "Run You Fools" insight).
7. **`scripts/fire_server.py`** (row 47): dry-run execution surface, CI installs flask for it; execd (08-30) is the live execution service — superseded or still the Phase-1 surface?
8. **`scripts/orderflow_monitor_up.sh` / `orderflow_alert_fmt.py` / `calibrate_oflow_thresholds.py`** (rows 27, 30, 43): the registry's own §4 retirement candidates, blocked on "confirm the sentinel covers every case" since 08-13.
9. **`.claude/agents/strader.md` + `intent.yaml`** (rows 60–61): April domain-factory profiles naming TradingView MCP tools and retired rules as current — delete, or does COO's registry still read them?
10. **`.beads.gate.lock`** (row 56): empty tracked file, zero references — a beads mechanism or a stray?
11. **`.env.template`** (row 62): could not be read (matches the `Read(.env.*)` deny) — someone with eyes on it should confirm it matches `strader/config.py`'s declared fields.
12. **`knowledge/schwab-auth-pattern.md`** (row 81) and its memory twin: canon page says schwab-py is "installed from PyPI (1.5.1)" and names `schwab-generate-token.py`; reality is the `lib/schwab-py` hobbled-readonly submodule and `scripts/refresh_schwab_token.py`. Steve's canon — rewrite or retire is his call.
13. **`strader/playbooks/options-premium-harvest.md`** (row 78 note): status `worthy`, "collect theta by selling options premium, 21–45 DTE" — an InvestiTrade catalogue import (st-c71, 07-02) now read as a strategy record by the canon loader; contradicts the long-premium-only profile. Strategy content is Steve's; the audit only flags the contradiction.
14. **Memory twins** (row 108): 23 memory files have a `graduated_from` canon page in `knowledge/`; two bodies have drifted from canon (schwab-auth, databento billing). Policy question: shrink indexed twins to one-line pointers, or keep full bodies?
15. **Unindexed behavioural corrections** (rows 100–103): `no_manufactured_precedent`, `no_self_confirming_callbacks`, `steve_risk_authority_not_pa_arbiter`, `corrections_persist_immediately` were cut from MEMORY.md on 08-17 and never graduated — they no longer load, so by Steve's test they prevent nothing; re-index, graduate, or delete.
16. **`runbook/mancini/commentary/2026-05-19.jsonl` and `2026-07-01.jsonl`** (row 66) and **`data/calls/`** (row 67): pre-pipeline samples with no test or doc citing them — golden fixtures someone remembers, or leftovers?

---

## 1. Dead scripts and modules

Scope: `scripts/`, `tools/`, `runbook/`, `market/`, `strader/`, `footprint-icm/`, `broker_schwab/`,
plus `daemon/`, `present/`, `mancini/`, `tui/`, `execd/`. Invocation universe checked: `import`/`from`
references, shell invocations, the 15 Strader crontab lines and 9 systemd units (all match
`COO/SCHEDULE.md`), `.claude/skills/*/SKILL.md`, `.claude/agents`, `.beads/PRIME.md`, `CLAUDE.md`.
Everything in `footprint-icm/`, `execd/`, `runbook/` and `tools/` is referenced by a scheduled path,
a skill, a live module or a test — no rows from those trees except the two noted.

### 1a. The 08-12 confirmed-dead tranche, re-verified (bead st-sl1f, OPEN)

| # | path | evidence | disposition | rationale |
|---|---|---|---|---|
| 1 | `scripts/_chain_es_backfill.sh` | last 2026-05-23 df0f3da; live refs 0; no test | delete | one-shot nohup chain for the July backfill; git holds it |
| 2 | `scripts/corpus_pull_gexbot.py` | 2026-05-22 e1913b2; refs 0; no test | delete | 5-endpoint shim superseded by `corpus_poll_gexbot.py` (systemd) |
| 3 | `scripts/eod_view_schwab.py` | 2026-05-22 f9e85ee; refs 0 (gate-hook comment only); no test | delete | May EOD framing; nothing runs it |
| 4 | `scripts/es_volume_profile.py` | 2026-07-24 ab43bf8; refs 1 (mention in watcher-v2 plan); no test | delete | offline reader over `history.py` dumps that nothing produces |
| 5 | `scripts/figures/foundation09_paradox.py` | 2026-08-01 12b4c6c; refs 0; no test | archive | its two SVGs are already spliced into doc 09; tag+delete keeps the generator reachable |
| 6 | `scripts/gexbot_distill.py` | 2026-05-21 41fb337; refs 1 (registry row); no test | delete | pre-collector probe; sole importer of `market/ingest/gexbot.py` (row 33) |
| 7 | `scripts/gexbot_probe.py` | 2026-05-21 41fb337; refs 1 (comment in `gexbot_ws_probe.py`); no test | delete | superseded by `entitlements_probe.py` + `gexbot_ws_probe.py`; the comment survives |
| 8 | `scripts/hello_databento.py` | 2026-07-02 bbbc3b9; refs 0; no test | delete | hello-world; live path is `corpus_stream_databento.py` |
| 9 | `scripts/measurement/day_type_classifier.py` | 2026-05-25 345ffa7; refs 0; no test | delete | May study; day-type gating was retired on the enriched corpus 08-19 (120973b) |
| 10 | `scripts/orderflow_edge_tests_r4.py` | 2026-08-08 fdaaece; refs 0; no test | archive | round 4 retired two-signal for the singleton lane; results doc stands without the script |
| 11 | `scripts/poll_to_excel.py` | 2026-05-22 f9e85ee; refs 1 (`README_EXCEL.md`); no test | delete | March Excel/xlwings integration, never used since |
| 12 | `scripts/stream_to_excel.py` | same | delete | same |
| 13 | `scripts/probe_databento_access.py` | 2026-07-02 bbbc3b9; refs 0; no test | delete | entitlement probe superseded by `config/entitlements.yaml` + `entitlements_probe.py` |
| 14 | `scripts/probe_databento_live.py` | same | delete | same |
| 15 | `scripts/validation_batch_st3vu.sh` | 2026-07-06 bf41a4f; refs 0; no test | delete | one-shot $7.80 batch driver, self-described as such |
| 16 | `daemon/` (`strader-session.sh`, `strader-watchers.sh`, `zgent-invoke.sh`, `strader-schedules.cron`) | 2026-03-08 11123f3 "Hydrate Strader zgent from domain factory"; refs 0 outside archived specs; deps absent: `/usr/local/bin/zgent-invoke`, user `gtuser`, `/var/moo/strader`, `dreader-narrative.jsonl` all missing | delete | domain-factory era; cannot run on this box; the crontab is generated from SCHEDULE.md now |
| 17 | `present/driver.sh` | 2026-05-17 063fe97; refs 1 (archived plan); calls `python -m present.cli` — no `present/cli.py` exists | delete | invokes a module that was never written |
| 18 | `mancini/codegen.py`, `compositor.py`, `gallery.py`, `persist.py`, `post_mortem.py`, `replay.py` | 2026-05-16/19; only importers are each other, rooted at `codegen.py`, which nothing imports; no tests | delete | the May slide/gallery pipeline; `mancini/parser.py` and `pine_emitter.py` are live (imported by `runbook/mancini/chart.py` + tests) and stay |
| 19 | `scripts/lexicon_render.py` | **08-12 verdict corrected**: last 2026-08-26 517e102; refs 0 but it is an on-demand desk renderer (`--no-open`), edited nine days ago | keep | invocation-by-hand is its design; not clutter |
| 20 | `scripts/measurement/orderflow_lead.py` | **corrected**: cited by `docs/measurement/orderflow-lead-2026-08-06.md` | keep (dormant) | reproducibility for a results doc; the 08-12 plan's own rule for measurement campaigns |
| 21 | `scripts/measurement/retro_gamma_cut.py` | **corrected**: cited by `retro-gamma-cut-2026-08-06.md` | keep (dormant) | same |
| 22 | `scripts/measurement/scare_dip_catalog.py` | **corrected**: cited by `post-entry-tape-study.md` | keep (dormant) | same |

### 1b. Newly found dead, and the 08-12 "ambiguous" set ruled

| # | path | evidence | disposition | rationale |
|---|---|---|---|---|
| 23 | `.claude/skills/strader/{daily-pnl-summary,entry-signal-evaluation,greeks-analysis,position-sizing,risk-limit-enforcement}/` (12 files) | 2026-03-08 11123f3; only invoker is `daemon/strader-schedules.cron` (row 16); nested one level too deep to be discovered as skills (absent from the session skill list); content is template boilerplate ("Gather required data / Process and analyze") | delete | domain-factory scaffolding; P&L summaries and risk-limit enforcement are not things Strader does |
| 24 | `present/regime.py`, `present/signals.py` (+ `tests/market/present/test_regime.py`, `test_signals.py`) | 2026-05-17; non-test importers 0; only consumer was row 17's nonexistent `present.cli` | archive (with tests) | tmux-pane formatters for a dashboard that never existed; `present/speech.py` is live and stays |
| 25 | `scripts/cron/capture-supervisor-wrapper.sh`, `capture-supervisor-session.sh`, `gexbot-supervisor-session.sh`, `gexbot-orderflow-1s-supervisor.sh` (+ `tests/scripts/test_capture_supervisor_wrapper.py`, `test_gexbot_collect_window.py`) | headers say `*/2 * * * *`; crontab 09-04 has no such lines; retired 08-13 for timers (ce6ddf7, e5c02cd "the */2 supervisors … were retired 08-13"); systemd services exec the pollers directly | archive — **UNSURE** | retired mechanism with tests still green; `test_gexbot_collect_window.py` may test the poller's window logic rather than the wrapper — check before archiving |
| 26 | `scripts/corpus_poll.py` | 2026-06-08 34e1c53; refs 4 (registry §4 retire candidate, estate plan, a docstring, `corpus_poll_gexbot.py` provenance note); no test | delete (loud stub first, then delete, per st-sl1f) | superseded twin; running it double-writes `schwab.jsonl`/`gexbot.jsonl` — the one dangerous callable in the set |
| 27 | `scripts/orderflow_monitor_up.sh` | 2026-08-07; registry §2.3 "superseded for the live watch by the sentinel", §4 retirement candidate; launches into `steves-desk` tmux | archive — **UNSURE** | registry blocked it on "confirm the sentinel covers every alerting case" 08-13; nobody has |
| 28 | `scripts/orderflow_edge_tests.py`, `orderflow_edge_tests_r3.py`, `orderflow_trainer_build.py` | 2026-08-08; reference only each other and `orderflow_hist_sweep.py`; no measurement doc names them; no tests | archive | a concluded campaign (round 2 → 4; two-signal retired); `orderflow_hist_sweep.py` stays (cited by `clock_family_traversal.py`, 08-20) |
| 29 | `scripts/moves_sweep.py` | 2026-07-28 621ea1e; refs 0; no test | archive | one-off atom/leg grader; its consumers (`orderflow-fundamental-units.md`) do not cite it |
| 30 | `scripts/calibrate_oflow_thresholds.py` | 2026-08-11 6bfaa88; refs 0; `docs/measurement/oflow-spike-thresholds-2026-08-11.md` exists but does not name it | archive — **UNSURE** | finding kept in the doc; the script may be wanted for re-calibration |
| 31 | `scripts/measurement/expected_move.py`, `build_eyeball_checklist.py`, `greek_snapshot_study.py`, `iv_pin_study.py` | 2026-05-25 / 07-02; live refs 0 each; no tests | archive | May/June one-offs whose docs (`v_day_eyeball_v0.md`, `greek-discovery-plan.md`) do not cite them; `iv_pin_study` is the only importer of `market/pricing/black_scholes.py` (row 37) |
| 32 | `strader/feeds/__init__.py` | 2026-07-02; `carried()` called by nothing; only mentions are its own docstring, `strader/__init__.py`, `strader/README.md` | delete — **UNSURE** | documented seam with zero consumers; deleting it means amending `strader/README.md` (row 78) |
| 33 | `market/ingest/gexbot.py` | 2026-05-21; importers: row 6 and row 32 only; the live collector uses `market/corpus/gexbot_stream.py`; no test | delete | falls with its two dead importers; CI's httpx note about it becomes stale |
| 34 | `market/indicators/{__init__,gex,gex_calc,registry}.py`, `market/entities/gex_profile.py` (+ `tests/market/indicators/test_gex.py`, `test_gex_calc.py`, `test_registry.py`, `tests/market/entities/test_gex_profile.py`) | 2026-05-17/20; non-test importers: `gex_calc` ← `gex_now.py`/`gex_series.py` (row 41) only; `gex` ↔ `registry` mutual; the pre-GexBot Schwab-chain GEX math | archive — **UNSURE** | estate-plan Decision 2 ("delete with their targets, or mark legacy so `801 passed` stops overstating"), unanswered since 08-12 |
| 35 | `market/entities/position.py` (+ `test_position.py`) | 2026-05-17 95c0a0b; non-test importers 0 | archive | same stratum |
| 36 | `market/ingest/{__init__,mancini,schwab}.py` (+ `tests/market/test_ingest.py`, `test_ingest_session.py`) | 2026-05-20; the package is imported only by rows 41's `gex_now.py`/`gex_series.py`; `mancini.py`/`schwab.py` are reached only via the package `__init__` | archive — **UNSURE** | same stratum; `market/ingest/databento.py` is live (`corpus_stream_databento.py`) and stays |
| 37 | `market/pricing/black_scholes.py` (+ test) | 2026-07-02; sole importer is row 31's `iv_pin_study.py` | archive — **UNSURE** | falls with the study, unless a pricing module is wanted for execd marks |
| 38 | `broker_schwab/readers/history.py` | 2026-07-24 ab43bf8; refs: own docstring + row 4; not one of the two gate-exempt readers, so the hook blocks running it anyway | archive — **UNSURE** | a working $TICK/$ADD history reader that nothing calls and the gate forbids |
| 39 | `strader/evaluate/{day_classifier,playbook_evaluator}.py` (+ 2 tests) | 2026-07-02; non-test importers: none outside the package | keep — **UNSURE** | design intent (playbook entity design, "implemented 07-02"); the canon loader (st-k5a8, 08-29) reads the same playbooks as data but does not use the evaluator |
| 40 | `market/orderflow/regime.py` (+ `test_regime.py`) | 2026-08-19 e5a9c01; non-test importers 0; `flush_watcher.py` does not import it; registry §3.4 discusses it | keep | recent touch, records Steve's 08-05 insight; test-only for now |
| 41 | `scripts/gex_now.py`, `scripts/gex_series.py`, `scripts/hello_schwab.py` | May 2026; registry §4 retirement candidates; **but** `tests/test_schwab_gate_hook.py` lines 49, 53–54, 172 use them as fixtures, and `schwab-gate.sh:91` skips a missing file — deleting `gex_now.py` flips the "transitive via broker_schwab → BLOCK" case to allow | keep until the fixture is moved under `tests/fixtures/`, then archive — **UNSURE** | load-bearing by accident; `level_interaction_read.py` (line 70) is a fixture too but is otherwise live |
| 42 | `scripts/cron/live-parity-wrapper.sh` + `scripts/live_parity_check.py` (+ 2 tests) | wrapper "NOT YET INSTALLED IN CRON"; crontab 09-04 confirms; st-hrwe OPEN since 08-12 | keep — **UNSURE** | the only live==replay acceptance check, never executed: install or retire |
| 43 | `scripts/orderflow_alert_fmt.py` | 2026-08-10; registry §2.3 "never in the pipe — only `tee` is downstream" | keep — **UNSURE** | registry's own "wire it or let it rot" |
| 44 | `scripts/level_watch.py` | 2026-08-06; registry §3.3 rules it vs `level_strength.py` | keep (dormant) | on-demand analyst tool per the registry |
| 45 | `scripts/desk/continuation_meter.py` | registry: dead since 2026-08-05 tmux death; sole source for the continuation question; has a test | keep (dormant) | registry ruling stands |
| 46 | `scripts/flush_watcher.py` | shadow-mode by design (st-rtuu); nothing schedules it; has a test | keep (dormant) | awaiting thresholds, not abandoned |
| 47 | `scripts/fire_server.py` | dry-run by design; CI installs flask for its 16 rail tests; `execd/__main__.py:123` cites it in a comment only | keep — **UNSURE** | execd (08-30, st-5qjq) is the live execution service; whether fire_server is superseded is a roadmap call (`docs/plans/2026-08-30-execution-engine-cli-v1-roadmap.md`) |
| 48 | `tui/` (16 files: Go Bubble Tea "flytui", `POLECAT-BRIEF.md`, `data/tv-screenshot.png`) | 2026-03-09/10 gt-flytui; module path `github.com/gastown/strader-flytui`; inbound refs 0 (only `.gitignore` `tui/flytui`); never built here | delete | a Gas Town-era March scaffold; the Go module name is itself a deprecated-name remnant (§5) |
| 49 | `scripts/live-footprint-up.sh` | 2026-08-20; superseded by `strader-drill-bridge.service` + `strader-footprint-feed.service` (both enabled); still referenced as the manual fallback by the service comment and row 25 | keep | documented fallback path; fix its "stable address /tmp/desk-live-footprint.html" claim if the systemd page differs |
| 50 | `scripts/speak_replay.py`, `scripts/day_browser.py` (+ template), `scripts/session_review.py` | test-only or doc-only inbound refs; all are on-demand drill/replay surfaces (`session_review` cited by `runbook/mancini/fetch.py`) | keep | operator tools run by hand, per drills-as-code |
| 51 | `tools/coach.py`, `tools/coach_point_check.mjs`, the other `tools/*.mjs` checks, `tools/precommit_size_guard.sh` | 0–2 inbound refs each; behavioural checks and an operator CLI (st-135m, 08-19) | keep | dev tooling invoked by hand via `nodecheck.sh` |
| 52 | `scripts/fetch_youtube_transcript.py` | 2026-08-10; cited by two canonical GexBot docs and the youtube plan | keep | on-demand ingestion tool for the transcript corpus |

Not rows, but verified alive-by-schedule or alive-by-import and therefore out of scope: every
`scripts/cron/*-wrapper.sh` in the crontab, `corpus_daily.py` (run through COO's timer wrapper, whose
header says the Strader copy `scripts/cron/corpus-daily-wrapper.sh` takes over on the 08-30 ack — both
copies currently exist; that is a hand-off in progress, not clutter), `corpus_pull_databento*.py`
(cited by `corpus_daily.py` and tests; the nightly OPRA pull was dropped 08-17 but the ad-hoc path is
kept on purpose), `corpus_backfill_databento.py`, `gexbot_hist_migrate.py`, all `scripts/measurement/*`
not listed above (each cited by a `docs/measurement` results page or live code), `regen_parity_snapshot.py`,
`record_schwab_shapes.py`, `execd_vault_init.py`, `mancini_backfill_levels.py`, `corpus_repair_doubled_day.py`,
`fix_gexbot_hist_gzip.py`, `corpus_duplicate_sweep.py` (09-02 repair tools, documented in the ledger).

## 2. Superseded configs

| # | path | evidence | disposition | rationale |
|---|---|---|---|---|
| 53 | `.claude/settings.json` allow list: `mcp__tradingview__*` (l.78), `mcp__fetch__fetch`, `mcp__github__*` (l.76–77), `Skill(checkpoint)` (l.82) | TV MCP removed 2026-05-20 (no `.mcp.json` since; `knowledge/tradingview-chart-interface.md`); no MCP config anywhere in `.claude/`, `.codex/`, `.vscode/`; `/checkpoint` skill deleted 07-23 (85ad2e5) | delete the four lines — settings are Steve's to land; put on a bead with the patch | permissions for tools that do not exist |
| 54 | `.claude/hooks/scripts/gc-mail-stub.sh` + its `PreToolUse` entry in `settings.json` (l.11) | header: guards the `gc` binary at `/usr/local/bin/gc`; binary gone since 08-21 (memory `feedback_gas_city_produced_nothing.md`, verified again: no `.gc` on disk); the same memory records it false-positives on any command containing `gc ` | delete hook + settings line (Steve lands; fold into row 53's patch) | a guard for a vanished binary that now costs time |
| 55 | `.claude/hooks/attic/schwab-gate.sh.pre-st-ad6p.bak` | 2026-08-16 9b90529; refs 0 | delete | a `.bak` inside git; `git show 9b90529^:…` is the same bytes |
| 56 | `.beads.gate.lock` | 2026-08-10 36f0a81; empty file; refs 0 | delete — **UNSURE** | may be a beads lock convention; nothing in the repo names it |
| 57 | `.codex/config.toml`, `.codex/hooks.json` | 2026-06-09 634ab33 "bd init"; Codex-CLI hooks calling `bd codex-hook`; no Codex use evidenced | keep — **UNSURE** | harmless bd-init output; delete only if Codex is confirmed unused |
| 58 | `.agents/skills/beads/` | same bd-init origin; referenced by `AGENTS.md` | keep | part of the bd onboarding surface |
| 59 | `.vscode/settings.json` (`*.profile.yaml` association) | domain-factory leftover line; otherwise current (`lib/schwab-py` path) | keep | one dead line is not worth a row on its own |
| 60 | `intent.yaml` | 2026-04-13 af878ee; "Opinionated intermediary over TradingView MCP"; capabilities = row 23's five skills; `rooms`/`bootPriority` for the domain-factory tmux; refs 1 (07-25 research survey) | delete | the April zgent profile; every mechanism it names is gone |
| 61 | `.claude/agents/strader.md` | 2026-04-14 31de420; `tools:` = ten `mcp__tradingview__*` tools; `ruleRefs:` `no-autonomous-orders`, `trading-intermediary` (both retired to `docs/retired-rules/` 08-17) | delete or rewrite — **UNSURE** | presents the dead MCP and retired rules as the agent definition; unknown whether COO's zgent registry still reads it |
| 62 | `.env.template` | not read (matches the `Read(.env.*)` deny); last commit not checked for the same reason | **UNSURE** | should match `strader/config.py`'s declared fields; a human check |
| 63 | `.github/workflows/ci.yml` comment block | says "the pytest config's default testpaths is just strader/tests" — `pyproject.toml` has set both since st-hw5e; also names `market/ingest/gexbot.py` (row 33) as why httpx is installed | keep; fix comment | config is right, its prose is stale |
| 64 | `.claude/state/tv-cdp-research.md` (gitignored, on disk, 2026-05-06) | TradingView CDP research for the MCP that died 05-20 | delete (untracked; no git tag needed) | dead-mechanism research sitting where the session hook looks for `snapshot.json` |

## 3. Orphaned experiment directories and Gas City remnants

| # | path | evidence | disposition | rationale |
|---|---|---|---|---|
| 65 | `mancini/archive/2026-05-18..21/` (35 files, 8.5 MB, 17 slide PNGs) | last 2026-07-02 5030f70 (moved in as "replay archives"); consumers were rows 18's `gallery.py`/`compositor.py`; refs 1 (07-25 research survey) | archive (tag `pre-prune-2026-09-04`, delete) | May slide/replay experiment; the letter texts are the only non-derivable part and the raw emails live in the blob pipeline now |
| 66 | `runbook/mancini/commentary/2026-05-19.jsonl`, `2026-07-01.jsonl` | the contiguous daily run starts 07-15; 05-19 is referenced only by `mancini/archive/2026-05-19/forecast_2026-05-19.json` (row 65); 07-01 by nothing; no test names either | archive — **UNSURE** | pre-pipeline samples unless someone knows them as goldens (`tests/runbook` fixtures are dated 08-05) |
| 67 | `data/calls/` (2 JSON, 2026-08-06) | call-scoring experiment (95ffbfc "score against the actual settle"); refs only `.gitignore` | archive — **UNSURE** | one afternoon's calls; nothing reads the directory |
| 68 | `README_EXCEL.md` | 2026-03-10 cd6c368; documents rows 11–12 | delete | Excel integration doc for scripts that go |
| 69 | `LuxAlgo_VolumeProfile.txt` | 2026-07-18 e87aaf6 (landed in a handoff commit); raw YouTube auto-transcript at repo root; refs 0 | delete (or move under `docs/gexbot/transcripts/` if wanted) | stray root-level paste |
| 70 | `session-review-2026-07-19.md` | root-level one-off; kept deliberately in b095363 "keep 7/19 session review"; refs 0 | archive (move to `archive/`) | belongs beside the DaysActivity archives, not at root |
| 71 | `archive/strader-to-coo-orb-backtester.md` | 2026-05-17 memo predating the a2a ledger; refs 0 | keep | already in the archive tier |
| 72 | `docs/audits/2026-08-12-code-estate/*.json` (768 KB) | machine evidence for a plan marked LIVE with an open bead | keep | the record this audit builds on |
| 73 | `docs/gexbot/screenshots/` (4.2 MB, 08-06..08-10) | evidence per `capture-protocol.md`; `orderflow_monitor.py` cites the dir | keep | orderflow-mastery view-side record |
| 74 | Gas City / Gas Town remnants: (a) `.gitignore:40–46` "Gas Town / Gas City (added by gt/gc)" block incl. `.gc/`, `tui/flytui`; (b) `.beads/hooks/on_{close,create,update}` header "can be reinstated via `gc hooks install` if Gas City returns to service"; (c) row 54's hook; (d) row 48's Go module path | `.gc/` verified absent on disk; no `gc` on PATH | fix in place: drop the `.gitignore` block (keep `__pycache__/`), trim the three hook comments to "replaced the gc-installed hook 2026-06-12 [co-zk7p]" | the standing ruling is delete on sight, never surface; these are the last mentions that read as if return is possible |
| 75 | Gas City mentions that are history and fine: `docs/plans/2026-08-12-code-estate-plan.md` (Decision 1 VOID block), `docs/reviews/2026-08-05-mancini-parse-review.md:66`, `docs/retired-rules/no-env-prefix-commands.md:53`, `archive/DaysActivity-2026-08-21.md`, `docs/a2a/inbox.md` rows, the 08-12 census JSON (`Strader/.gc/...` paths of trees now gone), `AGENTS.md` links to `github.com/gastownhall` (the upstream beads project, not the experiment) | — | keep | post-mortems and ledger rows |

## 4. Stale documentation

Deliberately kept without rows: `docs/retired-rules/` (the retired tier, by design), `docs/a2a/`
(ledger), `docs/archive/plans|specs` (archived 08-14 on purpose, 02701bc), `knowledge/` (canon —
only pages describing a dead mechanism are listed), all `docs/measurement/*` results pages, and
`docs/tv-chart-configuration.md` (describes Steve's *manual* chart setup as observed 08-26 — not the
screenshot pipeline).

| # | path | evidence | disposition | rationale |
|---|---|---|---|---|
| 76 | `runbook/README.md` §"#3 chart — what's built vs manual" (l.~90–97) | "`apply_plan()` carrying both tradingview-mcp delivery paths … The live apply step needs TradingView Desktop + the `tradingview-mcp` server"; also opens with a COO design-spec path | fix in place | presents the MCP that died 05-20 as the delivery path; the Pine payload actually goes to the clipboard (`--clip`) and Steve pastes it (`docs/playbooks/mancini-renderer-daily.md`) |
| 77 | `runbook/mancini/chart.py` docstring l.5–9 and `apply_plan()` l.109–115 | "A delivery-agnostic plan for tradingview-mcp … hand to `pine_set_source`" | fix comment | code is live (run.py writes the Pine); the comment names a dead consumer |
| 78 | `strader/README.md` | describes `feeds/` as the layout (row 32); "Butterflies remain a manual, reference-only play" (CLAUDE.md lists late-day flies as a primary play); points at COO-repo specs for design of record; test command runs only `strader/tests/` (pyproject runs both trees since st-hw5e) | fix in place | the 07-02 fold-in README, not updated since; note also `strader/playbooks/options-premium-harvest.md` (Unsure 13) |
| 79 | `.claude/skills/tap-in/SKILL.md:317` "`/checkpoint` — Auto-save between handoffs" | skill deleted 2026-07-23 (85ad2e5) | fix line | a loaded skill advertising a dead one |
| 80 | `knowledge/checkpoint-loop-discontinued.md:16` "The checkpoint skill still exists for manual use only" | same deletion | fix line (canon page stays) | the decision is right, one sentence is now false |
| 81 | `knowledge/schwab-auth-pattern.md` (runbook type) | "schwab-py: Installed from PyPI (1.5.1)"; "`schwab-generate-token.py`" (no such file; `scripts/refresh_schwab_token.py` exists); `.gitmodules` has `lib/schwab-py` on branch `hobbled-readonly`; CLAUDE.md's Schwab section contradicts it | fix — **UNSURE** | canon describing a mechanism that was replaced; Steve's page to rewrite or retire (memory twin row 106 has the same text) |
| 82 | `docs/live-monitoring-registry.md` | §2 "observed 2026-08-13"; rows still name `live-footprint-up.sh` as the starter (now systemd, 08-16/08-29), `continuation_meter` "currently dead", corpus_poll/gex_now as §4 candidates (rows 26, 41) | keep; refresh the observation date and §2.2 rows | the authority table (§1) is still correct; the inventory is three weeks old |
| 83 | `docs/gexbot/orderflow-monitor.md` "Pieces" table + "Live mode tails it" | names `orderflow_monitor_up.sh` (row 27) and live mode as current; registry §1/§3.1 rule the monitor replay-only, sentinel live | fix the launcher/live rows | doc and registry disagree on the same script |
| 84 | `CurrentStatus.md:3,5` "Role: SPX Options Trading Intelligence (Consumer tier)", "Status: zgent (in-process toward certification)" | domain-factory-era header carried through every `/handoff` refresh; no certification process exists in the repo or SCHEDULE | fix two lines | a loaded surface repeating a dead framing |
| 85 | `docs/superpowers/plans/2026-07-25-footprint-replay-drills.md` | banner "STATUS 2026-08-14 — NOT STARTED, 36 checkboxes never ticked"; bead st-055 P1 OPEN, started 07-25, last update 08-24 | keep — **UNSURE** | the bead is open so the plan is not orphaned, but it has not moved in six weeks |
| 86 | `docs/superpowers/specs/2026-08-02-execution-gate-plan.md` | banner "OPEN, partially overtaken"; st-5ey OPEN; execd (08-30) now exists | keep | banner already says it; execd's README is the design of record now |
| 87 | `docs/plans/2026-08-12-code-estate-plan.md` | STATUS LIVE; its "delete 22" tranche and Decision 2 unexecuted; "rerun the full audit in mid-September" — this document is that rerun | keep | record this audit against it |
| 88 | `docs/gexbot/README.md`, `knowledge/tradingview-chart-interface.md`, `config/entitlements.yaml:261` | all describe the dead TV interface *as dead* | keep | correct; listed so the reader knows they were checked |

## 5. Deprecated names presented as current vs. as history

Sweeps: "Gas City|Gas Town|gastown", "zgent[_-]core", "Google Drive|gdrive|Drive bridge|/mnt/g/",
`zgent-bridge/(co|st|cd)/` and `(co|st|cd)/inbox`, "TradingView MCP|mcp__tradingview|tv_capture|
domain-factory|gtuser|/checkpoint|zgent-invoke", over the 1,045-file text universe and the memory
directory. **zgent_core: zero mentions anywhere.** **Google Drive as transport: one mention, a 09-01
ledger row explicitly calling it "the retired Drive bridge — one-off drop, not a revival".** **Short
folder names co/ st/ cd/: only `docs/retired-rules/zgent-permissions.md` (retired tier) and ledger rows;
the tap-in skill was updated to full names 08-24 (co-2fa6a) — verified clean.**

| mention | label | disposition |
|---|---|---|
| `intent.yaml:4,52` "intermediary over TradingView MCP", `mcp__tradingview__quote_get` | CURRENT (defect) | see row 60 |
| `.claude/agents/strader.md:4,7–16,34` TV MCP tool list, `ruleRefs` to retired rules | CURRENT (defect) | see row 61 |
| `.claude/settings.json:78` `mcp__tradingview__*`; `:82` `Skill(checkpoint)` | CURRENT (defect) | see row 53 |
| `runbook/README.md` #3 chart; `runbook/mancini/chart.py:5–9,110–115` tradingview-mcp | CURRENT (defect) | see rows 76–77 |
| `daemon/*` domain-factory, `zgent-invoke`, `gtuser` | CURRENT (defect) | see row 16 |
| `.beads/hooks/on_*:5` "if Gas City returns to service" | CURRENT (defect — reads as possible) | see row 74 (new) |
| `.gitignore:40` "Gas Town / Gas City (added by gt/gc)" | CURRENT (defect) | see row 74 (new) |
| `tui/go.mod`, `tui/**/*.go` `github.com/gastown/strader-flytui` | CURRENT (defect) | see row 48 |
| `.claude/skills/tap-in/SKILL.md:317` `/checkpoint` | CURRENT (defect) | see row 79 |
| `knowledge/checkpoint-loop-discontinued.md:16` "skill still exists" | CURRENT (defect) | see row 80 |
| `CurrentStatus.md:3,5` Consumer tier / certification | CURRENT (defect) | see row 84 |
| `gc-mail-stub.sh` header (Gas City deprecated 07-29) | HISTORY inside a hook that itself goes | see row 54 |
| `docs/plans/2026-08-12-code-estate-plan.md` Decision 1 VOID | HISTORY | keep |
| `docs/reviews/2026-08-05-mancini-parse-review.md:66` | HISTORY | keep |
| `docs/retired-rules/{no-env-prefix-commands,zgent-permissions,trading-intermediary}.md` (gc, co/st/cd, TV MCP) | HISTORY (retired tier) | keep |
| `docs/archive/specs/2026-05-17-market-entity-model-design.md:268` "tools/ — tv_capture" | HISTORY (archived spec) | keep |
| `docs/research/2026-07-25-trade-language-entity-survey.md:355` flags the old CLAUDE.md MCP claim | HISTORY | keep |
| `knowledge/log.md:11,31` rename of the screenshot-pipeline concept | HISTORY | keep |
| `docs/a2a/inbox.md:333` "(Google Drive) … retired Drive bridge" | HISTORY (ledger) | keep |
| `archive/DaysActivity-2026-08-21.md` Gas City voided; 08-12 census `Strader/.gc/*` rows | HISTORY (log / evidence) | keep |

## 6. Memory clutter

`/root/.claude/projects/-root-projects-Strader/memory/`: 52 files; `MEMORY.md` 36 lines indexing 35;
16 files unindexed since the 08-17 cut (`archive/MEMORY-index-2026-08-17.md` is the pre-cut index);
23 files have a `graduated_from:` twin in `knowledge/`. `CLAUDE.md` (08-17 rewrite) carries no
false-start notes — its one "learned" line (bd create positional, st-kq8) is correct and still
prevents the mistake; `.claude/rules/schwab-api-gate.md`'s struck-through corrected claim (st-ad6p) is
the same kind and stays.

| # | file | evidence | disposition | rationale (Steve's test: does it still prevent a repeat?) |
|---|---|---|---|---|
| 89 | `feedback_checkpoint_discontinued.md` | unindexed; graduated to `knowledge/checkpoint-loop-discontinued.md`; says "the checkpoint skill still exists" (false since 07-23) | delete | canon carries it; the memory body is wrong |
| 90 | `feedback_fork_doctrine.md` | unindexed; graduated | delete | duplicate of canon, never loads |
| 91 | `feedback_establish_before_abbreviate.md` | unindexed; graduated | delete | same |
| 92 | `feedback_probabilistic_not_absolutist.md` | unindexed; graduated | delete | same |
| 93 | `feedback_spell_out_bead_references.md` | unindexed; graduated | delete | same |
| 94 | `feedback_stages_not_beats.md` | unindexed; graduated | delete | same |
| 95 | `project_grow_into_live.md` | unindexed; graduated; the 8/1 go-live date it frames has passed | delete | canon page keeps the "grow into it" ruling |
| 96 | `project_foundation_comprehension_checks.md` | unindexed; "COMPLETE 2026-07-18 — protocol retired"; the retirement is logged in `knowledge/log.md` | delete | a done-marker that no longer loads |
| 97 | `project_counter_dictum_program.md` | unindexed; a TABLED pointer duplicating the banner on `knowledge/counter-dictum-program.md` | delete | duplicate |
| 98 | `project_beads_recovery.md` | unindexed; RESOLVED 06-09; its one durable line ("source of truth is `.beads/issues.jsonl` in git, never `bd bootstrap`") is cited by `knowledge/log.md` | keep — **UNSURE** | the hazard line is exactly the kind Steve keeps, but unindexed it prevents nothing; re-index one line or fold into a knowledge runbook |
| 99 | `project_pin_projection_research.md` (4.9 KB) | unindexed; June research plan ("wants a bead") plus the first `fly_replay_batch` result (day-selection edge, 246 days); three knowledge pages cite it as a memory ref | keep the result paragraph, drop the plan — **UNSURE** | a genuine strategy post-mortem wrapped in a stale to-do list |
| 100 | `feedback_act_on_cheap_reversible_actions.md` | unindexed, but `.beads/PRIME.md` (loaded every session) cites it by filename | keep; re-index one line | the Obvious Doctrine is the chronic one |
| 101 | `feedback_corrections_persist_immediately.md` | unindexed (indexed 08-16 by COO audit co-03ojd.1, dropped again at the 08-17 cut) | keep; re-index — **UNSURE** | Steve called the failure "distressing"; a correction that never loads cannot prevent the repeat |
| 102 | `feedback_no_manufactured_precedent.md`, `feedback_no_self_confirming_callbacks.md` | unindexed; not graduated; behavioural corrections from 07-30/08-06 | **UNSURE** (re-index or graduate) | same argument as row 101 |
| 103 | `feedback_steve_risk_authority_not_pa_arbiter.md` | unindexed; not graduated; the 07-27 role ruling (hindsight grades PA, not Steve's chair time) | **UNSURE** (graduate to `knowledge/` operator-profile) | a role ruling that shapes drill/label design; belongs in canon if it still stands |
| 104 | `feedback_no_deadline_countdown_nagging.md` | indexed; body records the 08-03 rule *and* Steve's 08-17 softening ("Strader took it too an extreme") | keep | the reversal is the value — it is why the countdown does not come back |
| 105 | `project_databento_live_collection.md` | indexed; body says "Live OPRA streaming is covered by … $199/mo"; contradicted by 3c46cb0 (08-17, no live OPRA entitlement) and bee583e (08-30, "no $199 Databento plan"); the knowledge twin was corrected 08-18 (73b70af) | fix body or reduce to a pointer | a loaded memory asserting an entitlement the registry says is gone — the exact drift `entitlements-registry.md` exists to stop |
| 106 | `project_schwab_auth_pattern.md` | indexed; same PyPI / `schwab-generate-token.py` text as row 81 | fix with row 81 | drifted in both stores |
| 107 | `feedback_review_docs_via_steves_desk.md` | indexed; names `desk-register.sh` / `desk-viewer.sh` / `marked --gfm`; CLAUDE.md names `desk-html.sh`; all three scripts exist in COO | keep — **UNSURE** | probably two routes, not drift; COO owns the mechanism |
| 108 | The 23 graduated twins still indexed (buying-movement, directional flies, singles, PAC, v-day, Carmine, zone equivalence, never-guess, drills-as-code, WSL, direction-inversion, perceptual, trading-since-2021, TV interface, schwab-auth, databento, …) | by design per CLAUDE.md ("auto-memory holds standing facts; keep its index short"); bodies duplicate canon and two have drifted (rows 105–106) | keep pointers; shrink bodies — **UNSURE** (policy) | one-line pointers cannot drift; full bodies already have |
| 109 | `feedback_gas_city_produced_nothing.md` | indexed; the "delete on sight, never archive, never a Steve decision" ruling | keep | it is the authority for rows 48, 54, 74 |
| 110 | `archive/MEMORY-index-2026-08-17.md` | the pre-cut index | keep | record of what was cut and why |

## 7. Commit archaeology

`git log` subjects matching revert / WIP / try / experiment / abandon / dead end / retire / remove / drop
(832 commits scanned; 44 matched; the load-bearing ones):

| commit | date | what | surviving files in HEAD | referenced now? |
|---|---|---|---|---|
| 9db7ae7 | 08-10 | Revert the 08-09 GexBot gate, supervisor and EOD ritual build | deleted for good: `.claude/skills/eod/`, `scripts/eod_packet.py`, `scripts/cron/eod-packet-wrapper.sh`, `tests/scripts/test_eod_packet.py`. Deleted-then-restored (69e0fcb "built/reverted/restored/corrected"): `strader/market_calendar.py` + test (live, imported by `corpus_poll_gexbot.py`), `scripts/cron/gexbot-supervisor-session.sh` (restored, then retired again 08-13 → row 25) | market_calendar yes; supervisor no |
| 5496175 | 07-02 | Remove dead TV-screenshot Vision experiment + superseded Gmail auth | `tools/tv_capture/*`, `runbook/mancini/llm.py` gone. Residue: one archived-spec line (history), `knowledge/log.md` (history) | clean |
| 85ad2e5 | 07-23 | Retire /checkpoint auto-save loop | `.claude/skills/checkpoint/` gone. Residue: `settings.json` `Skill(checkpoint)` (row 53), tap-in:317 (row 79), knowledge page l.16 (row 80), memory row 89 | four stale mentions |
| ce6ddf7 / e5c02cd | 08-13 / 08-17 | Collectors cut over to timers; */2 supervisors retired; health-assessors timer | the four supervisor wrappers + two tests still in HEAD (row 25) | tests only |
| a821568 | 08-17 | Refocus: 12 rules → 6, retired to `docs/retired-rules/` | residue: `.claude/agents/strader.md` `ruleRefs` to `no-autonomous-orders`, `trading-intermediary` (row 61) | one stale file |
| 5996348 | 08-12 | Code-estate plan — catalog, test, review, retire | `docs/audits/2026-08-12-code-estate/*`, plan; the "delete 22" tranche never executed (st-sl1f OPEN) | this audit |
| 75463fa / a7faa9e / fdaaece | 08-08 | Edge tests rounds 2–4; two-signal retired for the singleton lane | `scripts/orderflow_edge_tests{,_r3,_r4}.py`, `orderflow_trainer_build.py` (rows 10, 28) | each other only |
| 176150b / a5bbaf3 | 08-07 / 08-16 | GC fossil removed from CLAUDE.md; PRIME.md drops Gas City | residue listed in §5 (`.gitignore`, `.beads/hooks` comments, `gc-mail-stub.sh`, `tui/` module name) | four remnants |
| 3c46cb0 | 08-17 | Drop the nightly SPXW OPRA pull ("no live OPRA entitlement since the plan swap") | `scripts/corpus_pull_databento.py` kept for the ad-hoc path (correct); memory row 105 still asserts live OPRA is covered | one stale memory |
| bee583e | 08-30 | "No $199 Databento plan … split-half discard gate retired" | same memory drift (row 105); no code residue found | — |
| 120973b / 57eec8c | 08-19 | Hour/day-type gate and fire-index damp retired on the enriched corpus | `docs/measurement/*-rederivation-2026-08-19.md` carry the retirements (keep — these are the post-mortems); `scripts/measurement/day_type_classifier.py` (row 9) is the May precursor | docs only |
| 4b8843d | 08-26 | Retire the hindsight denylist in `present/speech.py` | no leftover; `speech.py` live | clean |
| c753150 | 09-01 | Session summary to the retired Drive bridge at Steve's request — "one-off drop, not a revival" | ledger row only; no Drive mechanism in code or config | clean |

## Method

**Universe.** `git -C /root/projects/Strader ls-files` (1,103 tracked files) minus `.venv/ lib/
node_modules/ .git/ data/ __pycache__/ .pytest_cache/` and binary types (`png jpg gz pdf ipynb`) →
1,045 text files, read as a NUL-separated list into `/usr/bin/grep -lF` via `xargs -0` (the real
grep, never the shim; no `-o`, no bounded repetition). Untracked: `git status --short` and
`git ls-files --others --exclude-standard` — nothing untracked outside `data/` and `.venv/` on
2026-09-04 except `.claude/state/tv-cdp-research.md` (row 64).

**Reference counting.** For every candidate: count of referencing files, a "live" count excluding
logs/ledger/census/self (definition at top), and a test count; for Python modules the dotted import
path was searched, and relative imports (`from . import x`, `from .x`) were checked by hand inside
`mancini/`, `runbook/mancini/`, `market/orderflow/`, `present/`. A helper listed non-test importers per
`market.*`/`strader.*` module across `scripts tools runbook market strader footprint-icm execd present`.
Tests named after a script were cross-checked separately because the "live" filter excludes any file
whose path contains the needle.

**Last-commit dates.** One `git log --format='%ad %h' --date=short --name-only` pass, first-seen per
path, saved to scratch and joined against `git cat-file -e HEAD:<path>`; spot rows re-verified with
`git log -1 -- <path>`.

**Scheduling.** `crontab -l` (15 Strader lines), `/etc/systemd/system/strader-*` (9 units, all
`enabled` per `systemctl is-enabled`), `ExecStart` lines read, and `/root/projects/COO/SCHEDULE.md`
(read-only) compared — every catalogued job resolves to a file in HEAD; no orphan schedule entries.
Each `scripts/cron/*.sh` was opened to see what it invokes (four `-m runbook.*` wrappers included).

**Skills/agents.** Every `SKILL.md`, `.claude/agents/strader.md`, `.beads/PRIME.md`, `CLAUDE.md`,
`AGENTS.md`, `.claude/rules/*` grepped for `scripts/ tools/ runbook/ …` paths and `-m` targets.

**Prior audit.** `docs/audits/2026-08-12-code-estate/dead-verdicts.json` parsed (301 verdicts:
103 confirmed-dead, 115 ambiguous, 83 alive-via); the 22 + 30 Strader entries re-verified individually;
beads st-sl1f, st-hrwe, st-055, st-5ey read with `bd show`.

**Deprecated names.** Sweeps listed in §5, over the tracked universe and the memory directory.

**Memory.** Directory listed; every `MEMORY.md` link resolved; unindexed files enumerated; the
`graduated_from:` field of every `knowledge/*.md` mapped back to memory; 30 of 52 memory files read in
full or first 40 lines; the rest are indexed one-liners whose descriptions were read from `MEMORY.md`.
`knowledge/index.md` links all resolve (34 files).

**Docs.** Heads of every top-level `docs/*.md`, all `README.md`s, `runbook/README.md` in full,
`docs/live-monitoring-registry.md` headings and tables, and STATUS banners across `docs/plans`,
`docs/superpowers`, `docs/proposals`, `docs/measurement`.

**Writes.** None inside the repo: the audit's only outputs are this file and four helper artefacts in
the scratchpad (`lastcommit.sh`, `refs.sh`, `universe.z`, `lastcommit-all.txt`); no git command that
writes was run and no tag exists (`git tag -l` is empty). `git status` at the end of the audit showed
modifications to `deploy/systemd/strader-capture*.service`, `docs/a2a/inbox.md` and `footprint-icm/*`
that were not present at the start — those belong to other agents active in the same session, not to
this audit.

**Not covered, deliberately.** (1) `.env.template` — deny-listed for reading. (2) `lib/schwab-py`
submodule internals. (3) `data/` (gitignored corpus) beyond the two tracked directories.
(4) The content quality of `docs/gexbot/canonical|community|transcripts` and `docs/foundation` —
treated as reference corpus and kept wholesale. (5) `archive/DaysActivity-*` — intentional log
archive. (6) COO-owned files, except reading `SCHEDULE.md` and one wrapper header to confirm the
corpus-daily hand-off. (7) Whether tests in the May-17 stratum still pass — not run; the question is
consumers, not greenness. (8) Bead hygiene (open beads whose subject is gone) — out of scope for a
file audit, but st-sl1f and st-hrwe are the two that this report's dispositions would close.
