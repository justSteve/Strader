{
  "mcpServers": {
    "tradestation": {
      "command": "npx",
      "args": [
        "-y",
        "@tradestation/mcp-server"
      ],
      "env": {
        "TS_API_KEY": "${TS_API_KEY}",
        "TS_API_SECRET": "${TS_API_SECRET}"
      },
      "type": "stdio"
    }
  }
}
