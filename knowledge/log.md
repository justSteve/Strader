---
type: convention
title: "Strader Knowledge Bundle — Log"
description: "Append-only history of bundle-level changes. Newest entries at the bottom. Every entry cites its authorizing bead."
timestamp: 2026-07-19T13:02:48-05:00
---

# Bundle Log (append-only)

- 2026-07-19 — **Bundle created** (co-czvg). Graduated 23 concepts from Claude Code auto-memory into OKF v0.1 form via `COO/tools/okf/graduate-memory.py`; content copied verbatim, only frontmatter transformed. Type vocabulary extended with `reference` and `operator-profile`. 3 files deliberately NOT graduated as work-state belonging in beads: project_beads_recovery.md, project_foundation_comprehension_checks.md, project_pin_projection_research.md. Typing is a first pass, open to revision by the owning zgent.
- 2026-08-02 — **`tradingview-screenshot-pipeline` → `tradingview-chart-interface`** (st-ysj7). Renamed and rewritten. The concept asserted that screenshots via `tools/tv_capture/tv_capture.py` were "the sole interface to chart state," but that tool was deleted 2026-07-02 in `5496175` (st-5xe) as a dead-end Vision experiment — so the concept named a nonexistent tool as the only path for six weeks. Corrected claim: BOTH automated approaches (MCP, screenshot→Vision) are dead, chart eyeballing goes through `tools/local_chart.py` against our own corpus, and the standing gap — nothing reads Steve's live chart — is now stated outright so it stops being rediscovered.
- 2026-08-05 — **`directional-gex-butterflies` gains a banned-framing section** (st-frco). Steve caught the range/theta fly framing for at least the fourth time (claude-monitor record: 2026-05-06, 2026-06-09, 2026-06-24 ×2, 2026-08-05). The concept was already correct; the failure was upstream of it — CLAUDE.md, always loaded and declared authoritative, said "centering butterflies relative to the consolidation range," so instructions outranked the bundle every session and the correction had to be re-issued by hand. This concept's own `Why` note had identified that in July and the source was never fixed. CLAUDE.md corrected in the same change: the fly is stated as directional movement-buying, body at the DESTINATION, delta not theta, with the precondition named as departure-and-return rather than range occupancy. Steve's three verbatim corrections recorded in the concept so the next agent can read the source rather than a paraphrase.
- 2026-08-04 — **`channel-family-taxonomy` added** (st-a3yh, epic st-2szg). New card from the continuation-program audit's systemic finding (§5.2): every caught oversight extended along the axis the operator last named; enumeration must happen over channel *families*, not instances. Ten families with per-family verdicts; clock/calendar/price-location/cross-index recorded as never-traversed, with clock the strongest single predictor the audit found (day-median AUC .875). Standing rule: study design traverses the list and writes a verdict per family before measuring; an empty family is a finding.

- 2026-08-09 — **`counter-dictum-program` added** (st-k68o), typed `decision`, and it is the bundle's first *charter*: the single canonical statement of what the current program is trying to do and why, rather than a distilled fact about how we work. Written because every memory store in this enterprise turns out to be repo-scoped — beads issues (st-ygy1 is invisible from COO), `bd remember` (COO 8 keys, Strader 4, no overlap), Claude auto-memory, CLAUDE.md, and both OKF bundles — so the program's *purpose* had been living only in COO's private auto-memory while Strader, the repo that executes the work, carried no statement of it at all. Two failures already traced to that split: COO's drift toward fly-lane assistance (needing Steve's 2026-08-08 singleton-scalp-proxy correction), and four orderflow edge-test rounds ignoring [`channel-family-taxonomy`](channel-family-taxonomy.md)'s binding traverse-first procedure while it sat in this bundle naming CLOCK as both the strongest predictor found and NEVER-TRAVERSED (st-1bv1 measured it: hour-of-day beats every signal those four rounds produced). Design is one source plus pointers — no content copied across repos, because this log already records two concepts that drifted when copies outranked originals. Pointers added to `bd remember` in both repos and to both auto-memory indexes. Carries Steve's 2026-08-09 reframe as its spine: *"Movement is abundant; direction is part of problem but greater is codifying an entry and stop loss strat"* — which promotes execution mechanics (Join The Turn st-chat, the harness st-ug5) ahead of the signal hunt. Bundle +1 file.

- 2026-08-10 — **`counter-dictum-program` TABLED** (st-k68o). Steve: the program is still under construction and its concepts "are not to effect our work product yet." The concept keeps its place in the bundle but carries a do-not-apply banner at the top, and the index entry leads with the same. Every pointer built for it on 08-09 was inverted the same day — `bd remember` in both COO and Strader (the `current-focus-counter-dictum` key now names the replacement focus and marks the charter parked), both auto-memory indexes, and COO's three fragment memories. This matters more than the banner: those surfaces were built specifically to reach every cold-start session, so leaving them pointing at tabled thinking would have injected it into exactly the fresh session Steve was opening to avoid it. Standing distinction recorded in the banner: the §5 measured ledger remains factually true — those studies ran and their numbers stand — but it is to be read as measurement history, not as direction. Replacement focus: understand GexBot's **Orderflow dataset** as guided by **Freddy** (`docs/gexbot/community/freddy_orderflow_series.md`, `docs/gexbot/orderflow-intended-read.md`, `docs/gexbot/canonical/`) — comprehension first, no edge hunt, no execution or stop design. Bundle file count unchanged.

- 2026-08-12 — **`databento-live-collection` billing/entitlements rewritten** (st-onr4, surfaced by the st-aski transcript review). The concept still asserted the 2026-06-08 state — "OPRA … $199/mo, Live data: Active" and "NO GLBX/ES live sub" — a full week after both facts inverted (OPRA plan cancelled ~08-04, replaced by CME/Futures; ES collected live via `--streams es,es-mbp1` since st-13pi/`f9515de`). The doc had been arguing the exact opposite of the code it describes. COO spotted it 08-11 but deferred the edit on bundle ownership (st-onr4 was filed *for* Strader and sat unactioned — a delegation-limbo instance the sync plan addresses). Both sections now carry the current shape with the superseded record preserved, and the standing rule is stated in-file: subscription facts are probed at statement time, never recalled from the bundle.

- 2026-08-13 — **`directional-gex-butterflies` brought current from COO's fly-doctrine rule** (st-zc38, Phase 0 of the sync plan st-aski; source bead co-y3fdk). The 08-05 banned-framing block did not hold, because it was installed one repo over from where the trading happens: the 2026-08-11 live session ran in COO, whose `CLAUDE.md` contained "butterfly"/"fly"/"flies" zero times, and Steve issued the same correction three more times in one afternoon. COO then wrote the newer specification (`COO/.claude/rules/fly-doctrine.md` plus a CLAUDE.md gate) — leaving the *canonical* concept older than its mirror, which breaks the single-home rule at its own test case. Canon is current again: the **X±12-or-further centering standard** with "five points from spot is still ATM" and the live `7760/7745/7730 PUT @1.70` order (body ~19 points from spot, and it paid as SPX ticked up toward it); the **never-reason-from-expiry** ban (breakevens, "+N to break even", dead zones, settlement tables, payoff-at-stall-point); the **$ per SPX point** replacement frame ($150 on $340 risked over a 1.6-point move ≈ $95/point, across the exact range being called a dead zone); the **orderflow-does-serve-flies** ban, scoping the 08-08 scalp-proxy redirect to what the *measurement program* targets rather than to what the instruments are good for; **price what he named**; four further banned phrases; the three verbatim 08-11 corrections; and the prohibitions-not-descriptions rationale (COO wrote the correct mechanism in its own notes at 13:08 and argued the opposite at 14:02, same session — retrieval was not the failure, so only an explicit ban survives). One substantive conflict was found and resolved in canon's favour rather than silently: COO's rule states flatly that he "does not hold to expiry", while canon has him leaving a runner for the pin that CAN ride toward expiration. Canon keeps the runner and scopes the ban to *evaluating or advising* the trade with expiry math — written as a scope note in the concept and echoed in [`buying-movement-delta-first`](buying-movement-delta-first.md) so its runner sentence cannot be read as a licence for breakeven talk. COO's copy needs the same scoping; flagged back to that side. Bundle file count unchanged.

- 2026-08-13 — **`entitlements-registry` added; billing figures moved OUT of the bundle** (st-g0or, Phase 1 item 4 of the sync plan st-aski). Subscription state now has one home, `config/entitlements.yaml`, read by `scripts/entitlements_probe.py`. The registry distinguishes two kinds of entry and the probe renders them in separate, labelled sections: PROBED lines are re-derived every run from local JSON state files and corpus listings — observations — while DATED lines are what Steve reported from a billing portal on a date, rendered with that date and its age so no reader can mistake one for the other. A green probed line proves the data is landing, never that the contract is paid. The probe calls no vendor API and the registry holds no credential (enforced by a test), because a check that needs credentials to answer "what are we entitled to" is unrunnable exactly when the answer matters. Two bundle concepts were emptied of restated figures and now point at the file instead: `databento-live-collection` (which had twice carried plan names and prices and twice gone stale — it asserted the cancelled OPRA sub as "Live data: Active" for a week, st-onr4) and `counter-dictum-program` §8 (tier name and downgrade date replaced by the review shape). This inverts the bundle's usual instinct: the durable thing here is not the fact but the *rule about where the fact lives*, because every one of the four incidents behind it — the OPRA→Futures swap, the GexBot resub, the State→Quant reveal, the OPRA cancellation both agents still assumed active — was a fact with no home that lived in several docs at once and rotted in all of them. Bundle +1 file.

- 2026-08-14 — **`orb-playbook` and `selective-range-scalping` added; CLAUDE.md cut 417 → 279 lines** (st-ylqw, approved by Steve 2026-08-14 off the desk review package). Steve's 08-13 ruling: *"The time spent worrying about the exact details of a trading strat do not belong in the Claude.md file because there isn't any action you could take that will depend on it."* The test he gave for membership in the always-loaded file — **does any action the agent takes depend on this fact?** — is what governed the cut. Strategy mechanics fail it; boundaries, voice, operator profile, instrument inventory, and infrastructure pass it and stayed. Also a framing constraint and not only a content one: he does not want responses shaped around strategy minutiae, because it is not a global, persistent set of facts he wants the agent worrying about. He directs the trading — *"I have a good handle in my own head about what i want to trade — i don't need you guys for that."* The replacement centre of gravity is now stated in the file as **The Mission — Price Action Literacy**: build his confidence reading continuation vs. pivot, with chart presentation and the drill/learning surfaces named as first-class product rather than as an afterthought to analysis.

  The acceptance criterion was that **every claim removed must already live in `knowledge/`** — a demotion that lands on nothing is a deletion. Claim-by-claim verification found the fly method, the banned framings, the trend-day-reversion caveat, the singles doctrine, and the strike-selection detail all present. It also found **two holes**, and both are why this entry adds files rather than only subtracting lines. Strategy 2 had no concept at all, which Steve's *"orb not dead"* ruling turned from a tidy-up into a blocking prerequisite; Strategy 3 had none either, since [`pac-order-blocks-for-strike-centering`](pac-order-blocks-for-strike-centering.md) covers PAC for fly centering only. `selective-range-scalping` keeps the **exploratory** status it carried in CLAUDE.md and was never promoted past — the concept records the status rather than laundering it into settled method by the act of being written down. Three claims were dropped rather than moved, and named as such: the $2.60 → $0.25 → $2.50 repricing walk (illustrative numbers; the mechanism lives in the fly concepts) and the seven "What We're Building Toward" goals (all strategy-optimization aims from April, superseded by the mission change — work direction belongs in beads, not here).

  The fly bans did **not** move to the bundle. They moved *up*, to a new loaded-tier rule `.claude/rules/fly-doctrine.md`, because this bundle's own history is the argument against trusting retrieval with a constraint: the 08-05 banned-framing block was correct and in place, and Steve still issued the same correction three more times on 08-11 (see the 2026-08-13 entry below). A constraint that only binds when retrieved does not bind. Canon stays in [`directional-gex-butterflies`](directional-gex-butterflies.md) and [`buying-movement-delta-first`](buying-movement-delta-first.md); the rule is enforcement and says so, including that canon wins any conflict and the rule is what gets fixed.

  One drift fix was folded in, flagged rather than smuggled: CLAUDE.md still named "TradingView MCP (owned)" as the primary instrument, which [`tradingview-chart-interface`](tradingview-chart-interface.md) recorded dead on 2026-07-26. Charts render from our own corpus via `tools/local_chart.py`. Cross-repo: Steve wants the same scope change applied to COO's `CLAUDE.md` — Strader drafts the scope, COO applies it to its own file, and the tier caveat travels with it (COO's cut is not Strader's cut; the shared thing is the test, not the line list). A2A memo sent the same day. Bundle +2 files.
- 2026-08-15 — **`orderflow-mastery-ownership` added** (st-ygy1; COO decision co-u58u2). Steve, in a COO session: "the Mastery of Orderflow was a mistake on my part. That needs to land back with Strader." The 08-06 directive, the 08-08 scalp-proxy scope, the 08-10 tabling and target, and the binding traverse-first method are now stated in one canonical file here; COO's auto-memory copy becomes a pointer. The directive is unchanged — only its owner and its home moved. Bundle +1 file.
- 2026-08-19 — **`reclaim-under-the-lid` added** (st-jhof). Steve read Carmine Rosato's morning posts (three swings at 7739 in 33 minutes, the last 7730 → 7750) against our tape and named what he saw: "a set of conditions as distinct as Mancini's FBD." Recorded as a playbook concept: six conditions in the order they became true (level with history; broken overnight, opened above; flush under and bought back; stepping-down lid under the level; higher lows with dips absorbed; thin above to the first shelf with volume), with the 08-19 morning as the worked example — bars, volume and bid/ask by price, Carmine's posts in CT, Mancini's 7738 trigger, yesterday's ceiling from the 08-18 corpus. Five of the six are mechanical; a recognizer is named as a follow-on, not built. Bundle +1 file.
- 2026-08-19 — **`reclaim-under-the-lid` corrected in kind** (st-jhof). Steve, reading the first version: "to my eye it's continuation upward from below." The concept had called the setup Mancini's failed-breakdown shape; wrong in kind — an FBD is a broken *low* recovered, and 39 was yesterday's ceiling, a resistance broken overnight, retested from above and taken back through. Mancini's letter itself says "reclaim" for 7738 and reserves "Failed Breakdown" for the 7716 shelf. Rewritten: the setup is continuation through a level with history once a lid and absorbed dips form under it; the FBD shape (the 7724 flush bought back) is the floor condition beneath it, not the setup. Numbers unchanged.
- 2026-08-20 — **`channel-family-taxonomy` clock verdict: `NEVER-TRAVERSED` → `MEASURED`** (st-1bv1). The concept had recorded CLOCK as the strongest single predictor the continuation audit found (day-median AUC .875 against the deliverable's .607) *and* as never traversed, while its own procedure makes traversal binding before measuring. Orderflow rounds 1–4 skipped it; the bundle named the answer and nobody read it. Now traversed over the 63-day 1s archive (2026-05-07..08-06, 63/63 days): P(≥10pt excursion in a 15-minute window) runs 81% in the 08:30 hour and falls 66 / 45 / 33 / 26 / 25 across 09–13, 27% at 14, median excursion 15.30 → 6.51 pts. The late-day flow concentration turns out to be the opposite of a movement signal — 97.7% of giant flow prints land in the 14:00 hour, where displacement is at its daily floor, because expiry mechanics move notional without moving price. Consequence recorded in the concept: round 4's pooled "≥10pt in ~40% of windows" comparator averaged across that 81-to-25 gradient and is **retired**; candidates score against their own hour. The reproduction is deliberately partial and says so — the 08-09 finding recorded its table but not its script, so the window measure had to be reconstructed (minute-sampled closes, exact at hours 12–14) and the window-inclusion rule was never recovered; it was left stated rather than fitted, because matching a target by tuning an unrecorded filter manufactures agreement instead of reproducing a result. Script committed alongside the table this time. Bundle 0 files (verdict edit).
- 2026-08-22 — **Stop-and-entry design un-tabled for the trigger desk** (st-xhxs, Steve's one word 'Go' on COO's recommended first move, ~11:05 CT). FD0 (st-apzt, Cut And Await) and the 0DTE harness exploration (st-ug5) resume as the trade execution engine's keyboard half; the **counter-dictum charter keeps its TABLED banner** until Steve names it. The first move in full: Steve records one full-day dictation and does the TOS fixture pass; COO builds the intent-dialect parser from the specimen into the st-79z.1 entity model; Whisper-local runs as a parallel one-evening experiment on the COO side; the phone order client (st-bxls) keeps waiting on the fire key. New under st-79z: Intent Dialect Parser (st-79z.3), Dictation Specimen Captured (st-79z.4), TOS Order Fixtures (st-79z.5). Context: COO desk page 2026-08-22-hybrid-voice-keyboard-ui-and-execution-engine-take (Reviews). Bundle file count unchanged.
- 2026-08-23 — **Intent dialect joined to FD0 as the bracket** (st-79z.3 × st-apzt). A priced *directional single* — a long put or a long call — now hands its contract to FD0's budget engine, which derives the counter-chase stop from its standing ceiling ($100, two attempts) at the live delta and returns the SPX-conditional exit fields Steve types into the TOS Order Rules gear: **above** spot for a put, **below** for a call. FD0's composer was generalized to take a caller-chosen contract, a `right` and `lots` (it always picked a put by delta band before); the bracket persists on the DayPlan so the one-line-per-process dictation pane can `price` in one process and `go` in the next. A **butterfly is defined-risk** — its loss is the debit the dialect already prints — so it stages unbracketed; verticals and condors are not priced yet. New file `strader/intent/bracket.py`; 12 new tests (137 in the intent+FD0 suite, 286 in strader/tests). The stop is tight by construction (≈1 SPX pt at 0.3δ) — right for a chase, and whether a futures-proxy single wants the same ceiling is Steve's one-line default to change. Bundle 0 files.
